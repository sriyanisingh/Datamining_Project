# 6. Data Visualization
fig, axes = plt.subplots(1, 2, figsize=(16, 6))

# Plot 1: Importance in Random Forest Model
importances = pd.Series(rf_model.feature_importances_, index=X_train.columns).sort_values(ascending=False).head(10)
sns.barplot(x=importances, y=importances.index, ax=axes[0], palette='viridis')
axes[0].set_title('Top 10 Feature Importances (Random Forest)')

# Plot 2: Clusters visualization on Engineered Features
sns.scatterplot(x='Academic_Consistency', y='Total_Skill_Score', hue='Cluster_KMeans', data=df, ax=axes[1], palette='Set1', alpha=0.6)
axes[1].set_title('K-Means Clusters: Academic Consistency vs Total Skill Score')

plt.tight_layout()
plt.show()
